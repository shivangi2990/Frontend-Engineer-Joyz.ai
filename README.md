# Frontend-Engineer-Joyz.ai
This is the Angular solution that displays a tree-like data structure using nested &lt;ul> and &lt;li> elements — with recursion handled via a recursive child component.
